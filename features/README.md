# Features Directory

This directory follows a Feature-Based Architecture (Domain-Driven Design). 
Each domain (e.g., `projects`, `datasets`, `kpis`, `ai-insights`) should encapsulate its own:
- Components
- Types
- Actions / Services
- Utils
